This is repo for university project.

For starting it:
    - install docker
    - install ng (Angular)
    - install npm
    - Go to folder where is docker-compose
    - execute docker-compose up -d --build
    - Go to frontend
    - Execute npm install
    - Execute npm start